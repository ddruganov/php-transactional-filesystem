version: "3.8"

services:
  transaction.fs.php:
    container_name: transaction.fs.php
    build: ./docker/php
    volumes:
      - ./:/app
    restart: unless-stopped
    command: [ "tail", "-f", "/dev/null" ]